[project]
name = "website"
version = "0.1.0"
description = "Sitio de información y herramientas en línea del residencial"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "django>=6.0.7",
]
