services:
  web:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - .:/app
      - venv:/app/.venv
    command: sh -c "uv run manage.py migrate && uv run manage.py runserver 0.0.0.0:8000"

volumes:
  venv:
